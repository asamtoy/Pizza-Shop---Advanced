<!-- This is where all the view stuff goes -->
<!-- As far as possible, we want to use HTML here; don't use Ruby to adjust the appearance! -->

<!-- If you use the ice cream cone function with the equal sign, it runs the ruby and puts it in.  Without equal sign, it just puts in the data with meaningful values. -->
<% for pizza in @pizzas do %>

<p> Name: <%= pizza.pretty_name() %> </p>
<p> Order: <%= pizza.quantity %> x <%= pizza.topping %> </p>
<p> <a href="/pizzas/<%= pizza.id %>">Show Order</a></p>
<p> <a href="/pizzas/<%= pizza.id %>/edit">Edit Order</a><p>
<form action="pizzas/<%= pizza.id %>/delete" method="POST">
  <input type="submit" value="DELETE"></form>

<hr/>

<!-- Need to put an end at the end; separate line, ice cream cones  -->
<% end %>
